# DockerCheatSheet-for-Devops
## This is a repository for DOCKER .

## This file is totally based on my Hand's On Experience.


**It covers 13 sections with all major Docker commands:**

- Installation & Version

- Images — pull, build, tag, inspect

- Containers — run, stop, remove, logs, stats

- Executing Commands in Containers — exec, interactive shell

- Volumes — create, mount, bind mounts

- Networks — create, connect, disconnect

- Docker Hub — login, push, pull, search

- Dockerfile Instructions — sample structure + instruction table

- Docker Compose — up, down, logs, scale + sample docker-compose.yml

- Copying Files — host ↔ container

- Saving & Loading Images — tar export/import

- System Cleanup — prune, system df

- Docker Swarm — init, deploy stacks, manage nodes
